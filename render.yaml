services:
  - type: worker
    name: arbitrage-telegram-bot
    runtime: python
    plan: background
    pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
